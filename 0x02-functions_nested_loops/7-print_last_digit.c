#include "holberton.h"
/**
 *print_last_digit - print the last digit
 *@n: display last digit
 *Return: 0;
 */
int print_last_digit(int n)
{
int last_num(int n);
if (n >= 0)
{
return (n % 10);
}
else
{
return ((n * -1) % 10);
}
}
